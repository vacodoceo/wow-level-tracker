import { makeStyles } from "@material-ui/core/styles";

const formsStyles = makeStyles((theme) => ({
  dialogText: {
    padding: 0,
  },
  loadingIcon: {
    marginRight: theme.spacing(1),
  },
}));

export default formsStyles;
