const programs = [
  {
    id: "leveling",
    label: "Nivelación"
  },
  {
    id: "pfo",
    label: "PFO"
  }
];

export interface Program {
  id: string;
  label: string;
}

export default programs;
